services:
  muf-dashboard:
    build: .
    ports:
      - "8080:8080"
    environment:
      # Maidenhead grid square - picks the two nearest of the ten curated
      # GIRO stations as hero tiles. Defaults to Frankfurt am Main if unset.
      - MUF_HOME_LOCATOR=
    volumes:
      - muf-data:/data
    restart: unless-stopped

volumes:
  muf-data:
